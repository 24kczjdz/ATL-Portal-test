# Environment Variables for ATL Website

# Database
MONGO_URI=mongodb+srv://atlab:0bhmfd2svCrNbQHd@atlab.69y5b.mongodb.net/ATLab-Admin?retryWrites=true&w=majority
JWT_SECRET=edvise-secret-key-2024

# Server
NODE_ENV=development
PORT=5000

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5000,https://atl-dashboard-one.vercel.app

# External ML API (Hugging Face Spaces)
ML_API_URL=https://candyyetszyu-atl-chatbot-api.hf.space/chat
HUGGINGFACE_API_URL=https://candyyetszyu-atl-chatbot-api.hf.space/chat

# HuggingFace API Token for Private Spaces
# ⚠️  SECURITY: Never commit the actual token to git!
# 
# To get your token:
# 1. Go to: https://huggingface.co/settings/tokens
# 2. Click "New token"
# 3. Give it a name (e.g., "ATL-Chatbot-API")
# 4. Select "Read" role
# 5. Copy the token (starts with "hf_")
#
# For Vercel Deployment:
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings → Environment Variables
# 3. Add variable: HUGGINGFACE_API_TOKEN
# 4. Value: your_actual_token (e.g., hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx)
# 5. Select all environments (Production, Preview, Development)
# 6. Save
#
# For Local Development:
# 1. Create a .env file in the root directory
# 2. Add: HUGGINGFACE_API_TOKEN=your_actual_token
# 3. Never commit .env to git
HUGGINGFACE_API_TOKEN=hf_your_token_here_replace_with_actual_token